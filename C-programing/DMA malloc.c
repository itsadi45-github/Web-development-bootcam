#include<stdlib.h>
#include<stdio.h>
int main()
{
    int n , i;
    float *p , sum=0 , avg;
    printf("\n How many course of a student:\t");
    scanf("%d",&n);
    printf("\nEnter marks of each course:\t");
    p=(float*)malloc(n*size of(float));
    for(i=0;i<n;i++){
        scanf("%f",(p+i));
        sum+=*(p+i);
    }

}
